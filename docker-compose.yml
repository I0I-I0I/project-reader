services:
    app:
        build: .
        container_name: project-reader
        ports:
            - "3000:3000"
        restart: always
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "3"
